import {
  createCanvas,
  Color,
  Vector2D,
  Angle,
  ConicalGradient,
  LinearGradient,
  RadialGradient,
  Shadow,
} from "webSpinner";

const canvas = createCanvas({});

document.body.appendChild(canvas);
